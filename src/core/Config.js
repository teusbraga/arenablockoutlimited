/**
 * Config.js: Re-exporta o objeto singleton CONFIG gerenciado pelo ConfigLoader.
 * Mantém compatibilidade com todos os módulos existentes do projeto.
 */

export { CONFIG, DEFAULT_CONFIG } from './ConfigLoader.js';